-- This Script Create my first table on my database
-- Create a new table inside hbtn_0c_0 DataBase if it
-- does not exist and add some descriptions
-- ID and Name.

CREATE TABLE IF NOT EXISTS first_table(
    id INT NOT NULL,
    name VARCHAR(256)
);    
